# Dependências da réplica hospedada (só leitura) — não inclui playwright.
# dashboard.py nunca importa scraper.py nem playwright: a coleta roda como
# subprocesso à parte, e na nuvem esse botão nem existe (CLOUD_MODE).
# Sem esta lista, o Streamlit Community Cloud usaria requirements.txt da raiz
# e instalaria o Playwright à toa — pacote pesado para um app que não coleta.
#
# Configurar no painel do app: Settings → Advanced settings → Python
# dependencies file → requirements-cloud.txt
pandas>=2.2
streamlit>=1.35
plotly>=5.22
openpyxl>=3.1
